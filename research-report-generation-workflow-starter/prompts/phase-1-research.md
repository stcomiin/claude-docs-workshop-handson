I want a technology scan of the current state of OCR (Optical Character Recognition) tools as of 2026.

Run the work in three steps.

Step 1 — Decompose the landscape.
Before searching anything, propose four meaningful slices of this topic that four parallel research agents could cover independently with minimal overlap. Slices could be by category, by technical approach, by use case, by maturity, or any clean division of the topic. Show me the four proposed slices in one short paragraph each, then proceed to Step 2.

Step 2 — Research in parallel.
Spawn 4 sub-agents, one per slice. Each should run independent web searches and return:

- The leading tools, products, or approaches in that slice
- What each one does well
- Known limitations
- Cost or licensing model
- Maturity
- Notable developments in the last 6-12 months

Step 3 — Synthesize.
Once all sub-agents return, produce a single markdown report with:

- Executive summary, 3-4 sentences
- Landscape overview, one paragraph per slice
- Comparison matrix, with rows as leading tools and columns as the dimensions you judge most useful
- Deep dive per slice, with short subsections per tool
- Recommendations by use case
- Sources and references

Save the report as `ocr_tech_scan.md` in the current directory.
