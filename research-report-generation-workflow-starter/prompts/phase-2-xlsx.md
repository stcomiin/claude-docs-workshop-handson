Using the xlsx skill, extract the comparison matrix from `ocr_tech_scan.md` into `ocr_tech_scan.xlsx`.

Requirements:

- Put the comparison matrix on the first worksheet.
- Add a second worksheet that groups tools by category.
- For the category worksheet, include pricing tier and best-fit use case.
- Format headers, freeze the top row, and make columns wide enough to scan.

Save the file as `ocr_tech_scan.xlsx` in the current directory.
