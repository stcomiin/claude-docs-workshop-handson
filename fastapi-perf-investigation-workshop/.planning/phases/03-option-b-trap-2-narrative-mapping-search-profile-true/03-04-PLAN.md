---
phase: 03-option-b-trap-2-narrative-mapping-search-profile-true
plan: "04"
type: gap_closure
wave: 3
depends_on:
  - "03-UAT"
files_modified:
  - README.md
  - reference/option-a-sample-run.md
  - tests/test_dashboard.py
autonomous: true
requirements:
  - REQ-investigation-uses-instrumentation
user_setup:
  - Docker must be available for live Elasticsearch command verification.
must_haves:
  truths:
    - "The README profile command is copyable against the workshop Elasticsearch runtime."
    - "Participant post-fix verification instructions do not conflict with shipped starting-state guard tests."
    - "The facilitator reference mirrors the corrected evidence path and verification guidance."
  artifacts:
    - path: .planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-UAT.md
      provides: "Diagnosed Phase 3 UAT gaps"
      contains: "Elasticsearch 9.3.3 rejects `POST /activities/_search?pretty&profile=true`"
---

<objective>
Close the Phase 3 UAT gaps found during live verification of Option B trap #2.

Purpose: Phase 3 added the right narrative shape, but live UAT found that the
profile command is not accepted by the workshop Elasticsearch runtime and that
the documented participant fix conflicts with the default pytest suite.

Output: README, facilitator reference, and tests aligned so the evidence path
and post-fix verification are executable.
</objective>

<context>
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-UAT.md
@README.md
@reference/option-a-sample-run.md
@tests/test_dashboard.py
@app/routes/dashboard.py
@docker/seed/mappings.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the profile evidence command</name>
  <files>README.md, reference/option-a-sample-run.md, tests/test_dashboard.py</files>
  <read_first>.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-UAT.md, README.md, reference/option-a-sample-run.md</read_first>
  <action>
    Replace the rejected `_search?profile=true` command form with the live-tested
    Elasticsearch 9.3.3-compatible form: keep `_search?pretty` in the URL and
    place `"profile": true` in the JSON request body. Update the facilitator
    reference to describe the same form. Update static tests so they guard the
    executable profile-body command rather than the rejected URL parameter.
  </action>
  <verify>
    <automated>curl -s -X POST 'http://localhost:9200/activities/_search?pretty' -H 'Content-Type: application/json' -d @/tmp/phase3-profile-body.json | uv run python -c 'import json,sys; assert "profile" in json.load(sys.stdin)'</automated>
    <automated>uv run pytest</automated>
    <automated>uv run ruff check app</automated>
    <automated>uv run mypy app</automated>
  </verify>
  <acceptance_criteria>
    - README contains `"profile": true` in the request body for the profile path.
    - The live profile command returns a response containing a `profile` section.
    - Reference guidance no longer tells facilitators to use the rejected query parameter.
  </acceptance_criteria>
  <done>The profile evidence path is copyable and verified against local Elasticsearch.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve participant post-fix pytest conflict</name>
  <files>README.md, reference/option-a-sample-run.md, tests/test_dashboard.py</files>
  <read_first>.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-UAT.md, tests/test_dashboard.py, README.md</read_first>
  <action>
    Split shipped starting-state guards from participant post-fix verification.
    Choose the least confusing project-local convention: for example, mark the
    pre-fix trap-shape test with a clear pytest marker and update the README and
    reference post-fix commands to exclude starting-state guards after the
    participant changes `username` to `username.keyword`. Preserve default
    shipped-branch verification for Phase 3 unless the docs explicitly state the
    post-fix command is different.
  </action>
  <verify>
    <automated>uv run pytest</automated>
    <automated>uv run ruff check app</automated>
    <automated>uv run mypy app</automated>
    <manual>In a detached temporary worktree, apply only the participant `username.keyword` fix and run the documented post-fix test command. It must pass.</manual>
    <manual>Confirm the shipped branch still has a guard proving trap #2 starts on parent `username` before participants fix it.</manual>
  </verify>
  <acceptance_criteria>
    - A participant who applies the documented one-line fix can run the documented post-fix gates successfully.
    - The shipped workshop state still has an automated or documented guard proving trap #2 starts on parent `username`.
    - README and reference use the same post-fix verification command.
  </acceptance_criteria>
  <done>Participant-facing verification and shipped-state guardrails no longer contradict each other.</done>
</task>

</tasks>

<verification>
Before declaring gap closure complete:
- [ ] `uv run pytest`
- [ ] `uv run ruff check app`
- [ ] `uv run mypy app`
- [ ] `test ! -e CLAUDE.md && test ! -e AGENTS.md`
- [ ] Live profile command returns a `profile` section.
- [ ] Slow-log enable/reset path still works.
- [ ] Temporary-worktree participant `username.keyword` fix passes the documented post-fix command.
</verification>
