---
phase: 03-option-b-trap-2-narrative-mapping-search-profile-true
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - tests/test_dashboard.py
autonomous: true
requirements:
  - REQ-investigation-uses-instrumentation
user_setup: []
must_haves:
  truths:
    - "Pytest statically verifies the Phase 3 README and reference coverage."
    - "Pytest continues to verify the shipped runtime starts before the participant fix for trap #2."
    - "Pytest continues to verify trap #3 remains present for Phase 4."
    - "Full Python quality gates stay green."
  artifacts:
    - path: tests/test_dashboard.py
      provides: "Phase 3 documentation and trap-boundary guards"
      contains: "test_phase3_readme_documents_profile_slow_log_and_keyword_fix"
  key_links:
    - from: tests/test_dashboard.py
      to: README.md
      via: "static docs coverage"
      pattern: "Option B: steps 9-10"
    - from: tests/test_dashboard.py
      to: reference/option-a-sample-run.md
      via: "facilitator reference coverage"
      pattern: "Option B steps 9-10"
---

<objective>
Add automated guards for Phase 3 docs and trap boundaries.

Purpose: The phase should not rely on prose alone. Tests should fail if the
README/reference omit instrumentation requirements or if execution accidentally
removes the pre-fix trap state before participants investigate it.

Output: Updated `tests/test_dashboard.py`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-PATTERNS.md
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-VALIDATION.md
@README.md
@reference/option-a-sample-run.md
@tests/test_dashboard.py
@app/routes/dashboard.py
@docker/seed/mappings.json
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03-07 | Tests could accidentally require live Elasticsearch credentials. | Medium | Keep tests static or fake-client only; no network calls. |
| T-03-08 | Tests could bless production-dangerous slow-log guidance without local scope. | Low | Assert docs include local workshop wording and reset commands where feasible. |
| T-03-09 | Tests could lock the Phase 4 anti-goal too broadly. | Low | Assert only the Phase 3 boundary strings and existing runtime trap shape. |

No high-severity threats remain because tests read local files and use the
existing fake ES client.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add README Phase 3 static assertions</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, README.md, .planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md</read_first>
  <action>Add a test function named `test_phase3_readme_documents_profile_slow_log_and_keyword_fix`. Use `Path("README.md").read_text(encoding="utf-8")` and assert the README contains these exact strings: `Option B: steps 9-10`, `_search?profile=true`, `index.search.slowlog.threshold.query.trace`, `_mapping?filter_path=*.mappings.properties.username`, `username.keyword`, `fix: aggregate on username.keyword to avoid fielddata on text field`, and `200-500 ms`. Also assert the README does not contain `fix: move date range to filter context, round now to day for cache hit` and does not contain `now-30d/d`.</action>
  <verify>
    <automated>pytest tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `def test_phase3_readme_documents_profile_slow_log_and_keyword_fix`
    - `tests/test_dashboard.py` contains `Option B: steps 9-10`
    - `tests/test_dashboard.py` contains `_search?profile=true`
    - `tests/test_dashboard.py` contains `index.search.slowlog.threshold.query.trace`
    - `pytest tests/test_dashboard.py -x` exits 0
  </acceptance_criteria>
  <done>Pytest protects README Phase 3 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Add facilitator reference static assertions</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, reference/option-a-sample-run.md, .planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md</read_first>
  <action>Add a test function named `test_phase3_reference_documents_trap2_path_and_trap3_boundary`. Use `Path("reference/option-a-sample-run.md").read_text(encoding="utf-8")` and assert it contains these exact strings: `Option B steps 9-10`, `Expected trap #2 investigation path`, `Profile or slow-log evidence`, `Mapping inspection`, `quoted profile or slow-log evidence`, `single aggregation-field change`, `Trap #3 remains`, `request-cache hits`, and `fix: aggregate on username.keyword to avoid fielddata on text field`. Assert it does not contain `fix: move date range to filter context, round now to day for cache hit` and does not contain `now-30d/d`.</action>
  <verify>
    <automated>pytest tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `def test_phase3_reference_documents_trap2_path_and_trap3_boundary`
    - `tests/test_dashboard.py` contains `Option B steps 9-10`
    - `tests/test_dashboard.py` contains `quoted profile or slow-log evidence`
    - `tests/test_dashboard.py` contains `Trap #3 remains`
    - `pytest tests/test_dashboard.py -x` exits 0
  </acceptance_criteria>
  <done>Pytest protects facilitator reference Phase 3 requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve runtime trap-shape guards and full gates</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, app/routes/dashboard.py, docker/seed/mappings.json, README.md, reference/option-a-sample-run.md</read_first>
  <action>Keep the existing tests `test_phase2_mapping_contains_username_text_fielddata_keyword_subfield` and `test_compute_org_summary_uses_query_context_unrounded_now_and_username_text_agg` intact. Add a lightweight guard named `test_phase3_no_agent_instruction_files_ship` that asserts `not Path("CLAUDE.md").exists()` and `not Path("AGENTS.md").exists()`. Do not add network calls. After test edits, run `pytest`, `ruff check app`, and `mypy app`.</action>
  <verify>
    <automated>pytest && ruff check app && mypy app</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` still contains `test_phase2_mapping_contains_username_text_fielddata_keyword_subfield`
    - `tests/test_dashboard.py` still contains `test_compute_org_summary_uses_query_context_unrounded_now_and_username_text_agg`
    - `tests/test_dashboard.py` contains `def test_phase3_no_agent_instruction_files_ship`
    - `pytest` exits 0
    - `ruff check app` exits 0
    - `mypy app` exits 0
  </acceptance_criteria>
  <done>Static docs coverage and runtime trap-boundary tests are green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_dashboard.py -x`
- [ ] `pytest`
- [ ] `ruff check app`
- [ ] `mypy app`
- [ ] `grep -q 'test_phase3_readme_documents_profile_slow_log_and_keyword_fix' tests/test_dashboard.py`
- [ ] `grep -q 'test_phase3_reference_documents_trap2_path_and_trap3_boundary' tests/test_dashboard.py`
- [ ] `grep -q 'test_phase3_no_agent_instruction_files_ship' tests/test_dashboard.py`
</verification>

<success_criteria>

- Tests assert README covers profile, slow-log, mapping inspection, keyword fix, and timing band.
- Tests assert the reference covers trap #2 path, model guidance, and trap #3 boundary.
- Existing runtime tests still protect `username` parent aggregation and query-context/non-rounded date trap.
- No `CLAUDE.md` or `AGENTS.md` exists.
- `pytest`, `ruff check app`, and `mypy app` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-03-SUMMARY.md`
</output>
