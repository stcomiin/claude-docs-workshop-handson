---
phase: 03-option-b-trap-2-narrative-mapping-search-profile-true
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - REQ-investigation-uses-instrumentation
user_setup:
  - service: docker
    why: "Live profile and slow-log commands require the local Elasticsearch Compose stack."
    local_dev:
      - "Start Docker Desktop or Docker Engine before manual live smoke checks."
must_haves:
  truths:
    - "README contains Option B steps 9 and 10 as a continuation after Option A."
    - "README requires `_search?profile=true` or slow-log evidence before accepting the trap #2 hypothesis."
    - "README documents mapping inspection showing `username` as text with `fielddata: true` and an existing `username.keyword` subfield."
    - "README names the second participant commit exactly."
    - "README stops Phase 3 after the `username.keyword` aggregation fix and leaves trap #3 for Phase 4."
  artifacts:
    - path: README.md
      provides: "Participant Option B trap #2 flow"
      contains: "Option B: steps 9-10"
  key_links:
    - from: README.md
      to: docker/seed/mappings.json
      via: "mapping inspection command and expected evidence"
      pattern: "fielddata"
    - from: README.md
      to: app/routes/dashboard.py
      via: "participant fix target"
      pattern: "username.keyword"
---

<objective>
Extend the participant README from Option A into Option B steps 9 and 10.

Purpose: Phase 3 is complete when a participant who finished Option A can prove
the remaining `compute_org_summary` bottleneck with Elasticsearch
instrumentation, identify the username mapping trap, apply the second narrow
fix, and stop before the cache-context trap.

Output: Updated `README.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-PATTERNS.md
@.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-VALIDATION.md
@.planning/phases/02-option-a-complete-vertical/02-03-SUMMARY.md
@README.md
@app/routes/dashboard.py
@docker/seed/mappings.json
@tests/bench.sh
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03-01 | Slow-log `0ms` settings could be copied into production guidance. | Medium | Scope commands to local workshop troubleshooting and include reset-to-`-1` command. |
| T-03-02 | Security-disabled Elasticsearch could be normalized. | Medium | Preserve the README note that ES security is disabled only for localhost workshop runtime. |
| T-03-03 | README could teach Phase 4 cache fixes early and collapse the workshop arc. | Low | Explicitly stop after the mapping fix and say trap #3 remains for the next step. |

No high-severity threats remain because this plan changes docs only, uses
localhost commands, and does not add auth, network exposure, or production
deployment instructions.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Option B steps 9-10 to README</name>
  <files>README.md</files>
  <read_first>README.md, .planning/intel/constraints.md, .planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md, app/routes/dashboard.py, docker/seed/mappings.json</read_first>
  <action>Update `README.md` so the opening description says the workshop includes the Option A path plus an Option B trap #2 continuation. After the existing `## Option A: 8-step investigation arc` section and its expected Option A commit, add a new section titled exactly `## Option B: steps 9-10`. In that section, add step 9 as: after the participant has made commit `fix: replace per-user _count loop with single terms aggregation`, restart uvicorn without `--reload`, run `bash tests/bench.sh`, read the uvicorn timer output, and confirm the dominant remaining cost has shifted to `compute_org_summary`. Require Claude to prove the next hypothesis using either `_search?profile=true` or the Elasticsearch slow log before proposing a fix. Add step 10 as: inspect `docker/seed/mappings.json`, find `username` mapped as `text` with `fielddata: true` and `fields.keyword.type == "keyword"`, change only the `username_distribution` aggregation field in `app/routes/dashboard.py` from `username` to `username.keyword`, run `pytest`, `ruff check app`, and `mypy app`, restart uvicorn, run `bash tests/bench.sh`, and create commit `fix: aggregate on username.keyword to avoid fielddata on text field`. State that no re-index is needed because the keyword subfield already exists in the multi-field. State that the calibration target after fix #2 is `200-500 ms`, but local pass/fail uses relative improvement plus profile/timer evidence.</action>
  <verify>
    <automated>grep -q 'Option B: steps 9-10' README.md && grep -q '_search?profile=true' README.md && grep -q 'fix: aggregate on username.keyword to avoid fielddata on text field' README.md && grep -q '200-500 ms' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Option B: steps 9-10`
    - `README.md` contains `_search?profile=true`
    - `README.md` contains `compute_org_summary`
    - `README.md` contains `username.keyword`
    - `README.md` contains `fix: aggregate on username.keyword to avoid fielddata on text field`
    - `README.md` contains `200-500 ms`
    - `README.md` contains `No re-index needed` or `no re-index needed`
  </acceptance_criteria>
  <done>README has the participant-facing Option B trap #2 flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile, slow-log, and mapping inspection commands</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md, app/routes/dashboard.py, docker/docker-compose.yml</read_first>
  <action>Inside the new `## Option B: steps 9-10` flow, include the exact profile command from `03-RESEARCH.md`: `curl -s -X POST 'http://localhost:9200/activities/_search?pretty&profile=true'` with `Content-Type: application/json` and a body containing `size: 0`, `track_total_hits: true`, a `query.bool.must` range on `created_at` with `gte: "now-30d"` and `lt: "now"`, and `aggs.username_distribution.terms.field` set to `username`. Include a mapping command: `curl -s 'http://localhost:9200/activities/_mapping?filter_path=*.mappings.properties.username' | python -m json.tool`. Include the exact expected mapping evidence strings: `"type": "text"`, `"fielddata": true`, and `"keyword": {"type": "keyword"}`. Include a slow-log alternative with `curl -s -X PUT 'http://localhost:9200/activities/_settings'` setting `"index.search.slowlog.threshold.query.trace": "0ms"` and `"index.search.slowlog.threshold.fetch.trace": "0ms"`, followed by `bash tests/bench.sh`, `docker compose -f docker/docker-compose.yml logs es | grep 'index.search.slowlog'`, and a reset command setting both thresholds to `"-1"`. Add a sentence that `0ms` logs every matching local search and is for this localhost workshop only.</action>
  <verify>
    <automated>grep -q 'index.search.slowlog.threshold.query.trace' README.md && grep -q 'docker compose -f docker/docker-compose.yml logs es' README.md && grep -Fq '_mapping?filter_path=*.mappings.properties.username' README.md && grep -q '"fielddata": true' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `curl -s -X POST 'http://localhost:9200/activities/_search?pretty&profile=true'`
    - `README.md` contains `"field": "username"` in the profile request body
    - `README.md` contains `_mapping?filter_path=*.mappings.properties.username`
    - `README.md` contains `index.search.slowlog.threshold.query.trace`
    - `README.md` contains `docker compose -f docker/docker-compose.yml logs es`
    - `README.md` contains `"-1"` in the slow-log reset command
  </acceptance_criteria>
  <done>README gives participants concrete ES instrumentation commands for trap #2.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the Phase 4 boundary in README</name>
  <files>README.md</files>
  <read_first>README.md, .planning/ROADMAP.md, .planning/intel/constraints.md, .planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-RESEARCH.md</read_first>
  <action>Add an explicit stop condition at the end of `## Option B: steps 9-10`: after fix #2, the endpoint is faster but the third trap remains. Say the participant should expect `bash tests/bench.sh` runs 2 and 3 not to show stable request-cache hits yet, and that diagnosing the query-context plus non-rounded date-math cache issue belongs to the next Option B step. Do not include the Phase 4 commit string `fix: move date range to filter context, round now to day for cache hit`, do not instruct the participant to move any range clause from `must` to `filter`, and do not tell them to change `now-30d` to `now-30d/d`.</action>
  <verify>
    <automated>grep -q 'third trap remains' README.md && grep -q 'request-cache hits' README.md && ! grep -q 'fix: move date range to filter context, round now to day for cache hit' README.md && ! grep -q 'now-30d/d' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `third trap remains`
    - `README.md` contains `request-cache hits`
    - `README.md` does not contain `fix: move date range to filter context, round now to day for cache hit`
    - `README.md` does not contain `now-30d/d`
  </acceptance_criteria>
  <done>README stops Phase 3 at trap #2 and preserves trap #3 for Phase 4.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q 'Option B: steps 9-10' README.md`
- [ ] `grep -q '_search?profile=true' README.md`
- [ ] `grep -q 'index.search.slowlog.threshold.query.trace' README.md`
- [ ] `grep -q 'fix: aggregate on username.keyword to avoid fielddata on text field' README.md`
- [ ] `grep -q '200-500 ms' README.md`
- [ ] `! grep -q 'fix: move date range to filter context, round now to day for cache hit' README.md`
- [ ] `! grep -q 'now-30d/d' README.md`
- [ ] `test ! -f CLAUDE.md && test ! -f AGENTS.md`
</verification>

<success_criteria>

- README contains Option B steps 9 and 10.
- README provides copyable `_search?profile=true`, slow-log, and mapping inspection commands.
- README names the exact second participant commit and post-fix-2 timing band.
- README states no re-index is needed because `username.keyword` already exists.
- README preserves the Phase 4 boundary and does not teach the cache-context fix.
- No `CLAUDE.md` or `AGENTS.md` exists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-option-b-trap-2-narrative-mapping-search-profile-true/03-01-SUMMARY.md`
</output>
