---
phase: 01-tracer-slice-workshop-motion-end-to-end
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/__init__.py
  - app/main.py
  - app/es.py
  - app/timing.py
  - app/routes/dashboard.py
  - tests/test_dashboard.py
autonomous: true
requirements:
  - REQ-correctness-preserved
  - REQ-no-out-of-scope-drift
user_setup: []
must_haves:
  truths:
    - "GET /dashboard/summary returns top_users and org_summary."
    - "The endpoint emits exactly three timer blocks named load_top_users, count_per_user, and compute_org_summary per request."
    - "Only trap #1 is present because count_per_user performs one ES count request per top user."
    - "compute_org_summary uses filter context and rounded date math in Phase 1."
    - "pytest, ruff check app, and mypy app are configured as phase gates."
  artifacts:
    - path: pyproject.toml
      provides: "Pinned FastAPI workshop Python project"
      contains: "fastapi==0.136.1"
    - path: app/main.py
      provides: "FastAPI app entry point"
      contains: "app = FastAPI"
    - path: app/timing.py
      provides: "Parseable timer context manager"
      contains: "[timer]"
    - path: app/routes/dashboard.py
      provides: "Slow tracer endpoint implementation"
      contains: "count_user_activities"
    - path: tests/test_dashboard.py
      provides: "Four correctness tests"
      contains: "test_response_shape"
  key_links:
    - from: app/main.py
      to: app/routes/dashboard.py
      via: "router include"
      pattern: "include_router"
    - from: app/routes/dashboard.py
      to: app/es.py
      via: "ES client helper"
      pattern: "get_es_client"
    - from: tests/test_dashboard.py
      to: app/main.py
      via: "FastAPI TestClient"
      pattern: "TestClient"
---

<objective>
Implement the Phase 1 FastAPI tracer endpoint, timer instrumentation, Python
project config, and correctness tests.

Purpose: The workshop must be runnable end-to-end before later phases deepen the
performance traps and documentation.

Output: A tested FastAPI app exposing `GET /dashboard/summary` with exactly the
response shape, timer names, and Phase 1 trap behavior defined by the roadmap.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-RESEARCH.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-VALIDATION.md
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-02-01 | The public endpoint could be mistaken for production-ready API guidance. | Medium | Keep README local-only language in Plan 03; app code contains no auth or production deployment guidance. |
| T-02-02 | A configurable ES URL could point to a non-local cluster during tests. | Medium | `app/es.py` defaults to `http://localhost:9200`; tests use a fake ES client and no real credentials. |
| T-02-03 | Generated response could expose sensitive data. | Low | Seed data is synthetic and endpoint returns only synthetic user IDs/usernames/counts. |

No high-severity threats remain because there is no user input, no auth surface,
no write API, and no real data source in the planned workshop runtime.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project config and app shell</name>
  <files>pyproject.toml, app/__init__.py, app/main.py, app/es.py</files>
  <read_first>.planning/intel/constraints.md, .planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-RESEARCH.md</read_first>
  <action>Create `pyproject.toml` for project name `fastapi-perf-investigation-workshop`, version `0.1.0`, `requires-python = ">=3.11"`, and a single `[project] dependencies` list containing `fastapi==0.136.1`, `uvicorn[standard]==0.46.0`, `elasticsearch==9.3.0`, `pytest==9.0.3`, `httpx==0.28.1`, `ruff==0.15.12`, and `mypy==1.20.2`. Keep the test/lint/type tools in the main dependency list so `uv pip install -e .` is sufficient for workshop participants. Configure `[tool.ruff]` with `target-version = "py311"` and `line-length = 88`. Configure `[tool.mypy]` with `python_version = "3.11"`, `strict = true`, and `packages = ["app"]`. Create `app/__init__.py` as an empty package marker. Create `app/main.py` with `app = FastAPI(title="FastAPI Perf Investigation Workshop")`, include the dashboard router, and expose `GET /healthz` returning `{"status": "ok"}`. Create `app/es.py` with `ES_URL_ENV = "ELASTICSEARCH_URL"`, `DEFAULT_ES_URL = "http://localhost:9200"`, and `get_es_client()` returning `Elasticsearch(os.getenv(ES_URL_ENV, DEFAULT_ES_URL), request_timeout=30)`.</action>
  <verify>grep -q 'fastapi==0.136.1' pyproject.toml && grep -q 'uvicorn\\[standard\\]==0.46.0' pyproject.toml && grep -q 'elasticsearch==9.3.0' pyproject.toml && grep -q 'app = FastAPI' app/main.py && grep -q 'DEFAULT_ES_URL = "http://localhost:9200"' app/es.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `fastapi==0.136.1`
    - `pyproject.toml` contains `uvicorn[standard]==0.46.0`
    - `pyproject.toml` contains `elasticsearch==9.3.0`
    - `pyproject.toml` contains `pytest==9.0.3`
    - `app/main.py` contains `app = FastAPI`
    - `app/es.py` contains `DEFAULT_ES_URL = "http://localhost:9200"`
  </acceptance_criteria>
  <done>The Python project installs the exact planned stack and exposes the app shell.</done>
</task>

<task type="auto">
  <name>Task 2: Implement timer and dashboard endpoint with trap #1 only</name>
  <files>app/timing.py, app/routes/dashboard.py</files>
  <read_first>app/main.py, app/es.py, .planning/intel/constraints.md, .planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-RESEARCH.md</read_first>
  <action>Create `app/timing.py` with a synchronous context manager `timer(name: str)` using `time.perf_counter()` and printing exactly `[timer] {name} {elapsed_ms:.1f}ms` to stdout on exit. Create `app/routes/dashboard.py` with `router = APIRouter(prefix="/dashboard", tags=["dashboard"])` and a `GET /summary` handler. Define functions `list_top_users(es, limit=10)`, `count_user_activities(es, user_id)`, and `compute_org_summary(es)`. The request handler must execute exactly these top-level timer blocks: `with timer("load_top_users")`, `with timer("count_per_user")`, and `with timer("compute_org_summary")`. `count_per_user` must loop over top users and call `count_user_activities` once per user, preserving the visible N+1 trap. `compute_org_summary` must use a `size: 0` ES search with a `bool.filter` date range, `gte: "now-30d/d"`, and `lt: "now/d"` for the 30-day slice; it must include a harmless `username_distribution` terms aggregation targeting field `username`, not `username.keyword`; it must return `total_activities_30d`, `unique_users_30d`, and `growth_rate_vs_prior_30d`. Do not use async/await. Do not add auth, frontend, vector, ML, ESQL, sharding, replicas, reindex, or production deployment code.</action>
  <verify>grep -q '\\[timer\\] {name} {elapsed_ms:.1f}ms' app/timing.py && grep -q 'timer("load_top_users")' app/routes/dashboard.py && grep -q 'timer("count_per_user")' app/routes/dashboard.py && grep -q 'timer("compute_org_summary")' app/routes/dashboard.py && grep -q 'count_user_activities' app/routes/dashboard.py && grep -q 'now-30d/d' app/routes/dashboard.py && grep -q '"filter"' app/routes/dashboard.py && ! grep -q 'async def' app/routes/dashboard.py</verify>
  <acceptance_criteria>
    - `app/timing.py` contains `[timer] {name} {elapsed_ms:.1f}ms`
    - `app/routes/dashboard.py` contains `timer("load_top_users")`
    - `app/routes/dashboard.py` contains `timer("count_per_user")`
    - `app/routes/dashboard.py` contains `timer("compute_org_summary")`
    - `app/routes/dashboard.py` contains `count_user_activities`
    - `app/routes/dashboard.py` contains `now-30d/d`
    - `app/routes/dashboard.py` contains `"filter"`
    - `app/routes/dashboard.py` does not contain `async def`
  </acceptance_criteria>
  <done>The endpoint returns real ES-derived summary data and contains only the visible N+1 performance trap.</done>
</task>

<task type="auto">
  <name>Task 3: Add four correctness tests with fake ES client</name>
  <files>tests/test_dashboard.py</files>
  <read_first>app/main.py, app/routes/dashboard.py, .planning/intel/constraints.md, .planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-VALIDATION.md</read_first>
  <action>Create `tests/test_dashboard.py` with exactly four pytest tests named `test_response_shape`, `test_top_users_sorted_descending`, `test_org_summary_numbers_are_non_negative`, and `test_response_is_json_serializable`. Use `fastapi.testclient.TestClient` against `app.main.app`. Monkeypatch the dashboard module's ES client factory so tests use a fake ES client and do not require Docker. The fake ES client must support the `search` and `count` calls used by `dashboard.py`, returning deterministic top users and org summary aggregation structures. Verify top-level keys are exactly `top_users` and `org_summary`; each top user has `user_id`, `username`, and `activity_count_30d`; org summary has `total_activities_30d`, `unique_users_30d`, and `growth_rate_vs_prior_30d`; top users sort descending by `activity_count_30d`; numeric fields are `int` or `float` and non-negative; and `json.dumps(response.json())` succeeds.</action>
  <verify>pytest</verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `def test_response_shape`
    - `tests/test_dashboard.py` contains `def test_top_users_sorted_descending`
    - `tests/test_dashboard.py` contains `def test_org_summary_numbers_are_non_negative`
    - `tests/test_dashboard.py` contains `def test_response_is_json_serializable`
    - `tests/test_dashboard.py` contains `TestClient`
    - `tests/test_dashboard.py` contains `monkeypatch`
    - `pytest` exits 0
    - `ruff check app` exits 0
    - `mypy app` exits 0
  </acceptance_criteria>
  <done>The correctness test gate exists and protects the endpoint shape without requiring live Elasticsearch.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest` exits 0
- [ ] `ruff check app` exits 0
- [ ] `mypy app` exits 0
- [ ] `grep -q 'timer("load_top_users")' app/routes/dashboard.py`
- [ ] `grep -q 'timer("count_per_user")' app/routes/dashboard.py`
- [ ] `grep -q 'timer("compute_org_summary")' app/routes/dashboard.py`
- [ ] `grep -q 'now-30d/d' app/routes/dashboard.py`
- [ ] `! grep -q 'async def' app/routes/dashboard.py`
</verification>

<success_criteria>

- The FastAPI app imports and serves `GET /dashboard/summary`.
- The endpoint returns the required `top_users` plus `org_summary` JSON shape.
- The endpoint emits exactly the three required timer names per request.
- The N+1 `_count` loop is present in `count_per_user`.
- Trap #2 is absent: no `fielddata` use and no `username.keyword` dependency.
- Trap #3 is absent: date filtering uses `filter` context and `now-30d/d`.
- The four correctness tests pass.
- No out-of-scope implementation topics are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-02-SUMMARY.md`
</output>
