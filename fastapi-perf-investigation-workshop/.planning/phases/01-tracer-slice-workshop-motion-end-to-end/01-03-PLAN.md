---
phase: 01-tracer-slice-workshop-motion-end-to-end
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/bench.sh
  - README.md
  - reference/option-a-sample-run.md
  - .gitignore
autonomous: true
requirements:
  - REQ-correctness-preserved
  - REQ-no-out-of-scope-drift
user_setup:
  - service: docker
    why: "README and bench smoke assume Docker daemon can run the ES container."
    local_dev:
      - "Start Docker Desktop or Docker Engine before live walkthrough verification."
must_haves:
  truths:
    - "tests/bench.sh runs exactly three curl requests to the localhost port 8765 dashboard summary endpoint."
    - "README.md includes Docker, three terminals, Python 3.11+, Git Bash/WSL for Windows, ports 9200/8765, and the --reload caveat."
    - "README.md includes the pivot prompt verbatim."
    - "README.md explicitly labels the content as tracer slice only and says Option A and Option B are not yet present."
    - "No CLAUDE.md or AGENTS.md is created."
  artifacts:
    - path: tests/bench.sh
      provides: "Three-run curl latency harness"
      contains: "Response time: %{time_total}s"
    - path: README.md
      provides: "Phase 1 setup and tracer walkthrough"
      contains: "tracer slice -- Option A and Option B are not yet present"
    - path: reference/option-a-sample-run.md
      provides: "Facilitator reference stub for later phases"
      contains: "Phase 1 tracer reference"
    - path: .gitignore
      provides: "Python/Docker workshop ignore rules"
      contains: "__pycache__/"
  key_links:
    - from: README.md
      to: docker/docker-compose.yml
      via: "setup command"
      pattern: "docker compose -f docker/docker-compose.yml up -d"
    - from: README.md
      to: tests/bench.sh
      via: "benchmark command"
      pattern: "bash tests/bench.sh"
    - from: tests/bench.sh
      to: app/routes/dashboard.py
      via: "localhost endpoint path"
      pattern: "/dashboard/summary"
---

<objective>
Add the participant-facing tracer instructions, bench harness, reference stub,
and ignore rules for the Phase 1 workshop slice.

Purpose: Phase 1 is successful only if a facilitator can run the workshop motion
end-to-end and see the pivot prompt applied to trap #1 without accidentally
teaching later Option A/B material.

Output: `tests/bench.sh`, `README.md`, `reference/option-a-sample-run.md`, and
`.gitignore`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-RESEARCH.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-PATTERNS.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-VALIDATION.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-01-SUMMARY.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-02-SUMMARY.md
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-03-01 | README could normalize disabling ES security outside localhost. | Medium | README must explicitly describe this as a local workshop runtime and not production guidance. |
| T-03-02 | Participants could paste commands from the wrong directory or hit a remote service. | Low | README commands must start from the workshop directory and use localhost ports 9200/8765. |
| T-03-03 | Reference stub could spoil future Option A/B traps. | Low | Keep Phase 1 reference to tracer behavior only and mark deeper runbook entries as future phases. |

No high-severity threats remain because docs and bench commands target only
localhost and contain no credentials or production deployment steps.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add bench harness and ignore rules</name>
  <files>tests/bench.sh, .gitignore</files>
  <read_first>.planning/intel/constraints.md, .planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-RESEARCH.md</read_first>
  <action>Create `tests/bench.sh` with shebang `#!/usr/bin/env bash`, `set -euo pipefail`, and exactly one `for i in 1 2 3; do` loop. Inside the loop, run `curl -s -w "Response time: %{time_total}s\n" -o /dev/null http://localhost:8765/dashboard/summary`. Mark the file executable. Create `.gitignore` with Python caches (`__pycache__/`, `*.py[cod]`), virtualenvs (`.venv/`, `venv/`), test/type/lint caches (`.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`), local env files (`.env`, `.env.*`), generated seed file (`docker/seed/documents.ndjson`), and editor/OS noise (`.DS_Store`). Do not ignore `.planning/`, `README.md`, `reference/`, app code, tests, or Docker source files.</action>
  <verify>test -x tests/bench.sh && grep -q 'for i in 1 2 3; do' tests/bench.sh && grep -q 'Response time: %{time_total}s' tests/bench.sh && grep -q 'http://localhost:8765/dashboard/summary' tests/bench.sh && grep -q '__pycache__/' .gitignore</verify>
  <acceptance_criteria>
    - `tests/bench.sh` is executable
    - `tests/bench.sh` contains `for i in 1 2 3; do`
    - `tests/bench.sh` contains `Response time: %{time_total}s`
    - `tests/bench.sh` contains `http://localhost:8765/dashboard/summary`
    - `.gitignore` contains `__pycache__/`
    - `.gitignore` contains `.venv/`
    - `.gitignore` contains `docker/seed/documents.ndjson`
  </acceptance_criteria>
  <done>The bench harness and ignore rules match the Phase 1 workshop contract.</done>
</task>

<task type="auto">
  <name>Task 2: Write README tracer setup and walkthrough</name>
  <files>README.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/intel/constraints.md, .planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-PATTERNS.md</read_first>
  <action>Create `README.md` titled `# FastAPI Perf Investigation Workshop`. Include a short "What this tracer slice includes" section listing Docker ES, 50k seed, FastAPI endpoint, three timer blocks, four pytest tests, bench harness, and trap #1 only. Include the exact sentence `This is the tracer slice -- Option A and Option B are not yet present.` Include setup prerequisites: Docker Desktop/Engine, three terminals (`uvicorn`, `claude`, `tests/bench.sh`/`pytest`), Python 3.11+, Git Bash or WSL for Windows, ES port 9200, FastAPI port 8765, and the caveat to use `uvicorn app.main:app --port 8765 --reload` only for first sanity check and manually restart uvicorn between bench runs after a fix. Include setup commands: `docker compose -f docker/docker-compose.yml up -d --build`, `uv pip install -e .` with `python -m pip install -e .` fallback, `uvicorn app.main:app --port 8765 --reload`, `curl http://localhost:8765/dashboard/summary`, `bash tests/bench.sh`, `pytest`, `ruff check app`, and `mypy app`. Include the pivot prompt verbatim: `Prove that hypothesis with data. Run \`bash tests/bench.sh\`, then read the timing log output from the uvicorn console. Tell me which named timer block dominates the wall-clock time, and quote the numbers verbatim. Only after you have the numbers, propose the fix.` Include a 10-minute facilitator walkthrough: start ES, start uvicorn, curl endpoint, run bench, ask Claude where time is going, use pivot prompt, observe `count_per_user`, apply only the N+1 terms-aggregation fix during the walkthrough, restart uvicorn, re-run bench, run pytest/ruff/mypy, and stop before Option A/B material. Include scope guardrails saying not to add async/await, auth, frontend, production deployment, ES cluster ops, reindex strategy, vector, ML, or ESQL.</action>
  <verify>grep -q 'This is the tracer slice -- Option A and Option B are not yet present.' README.md && grep -q 'Prove that hypothesis with data. Run `bash tests/bench.sh`' README.md && grep -q 'uvicorn app.main:app --port 8765 --reload' README.md && grep -q 'Git Bash or WSL' README.md && grep -q 'ruff check app' README.md && grep -q 'mypy app' README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `This is the tracer slice -- Option A and Option B are not yet present.`
    - `README.md` contains `Prove that hypothesis with data. Run \`bash tests/bench.sh\``
    - `README.md` contains `uvicorn app.main:app --port 8765 --reload`
    - `README.md` contains `Git Bash or WSL`
    - `README.md` contains `port 9200`
    - `README.md` contains `port 8765`
    - `README.md` contains `ruff check app`
    - `README.md` contains `mypy app`
    - `README.md` does not contain `async/await refactor`
  </acceptance_criteria>
  <done>The README supports the Phase 1 walkthrough and preserves later Option A/B scope.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 1 reference stub and final smoke checks</name>
  <files>reference/option-a-sample-run.md</files>
  <read_first>README.md, tests/bench.sh, .planning/intel/constraints.md, .planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-PATTERNS.md</read_first>
  <action>Create `reference/option-a-sample-run.md` titled `# Phase 1 tracer reference`. State that it is a facilitator reference for the tracer slice only, not the full Option A answer key. Include expected starting commands, expected timer names (`load_top_users`, `count_per_user`, `compute_org_summary`), expected dominant block (`count_per_user` before the N+1 fix), expected verification commands (`pytest`, `ruff check app`, `mypy app`, `bash tests/bench.sh`), and a "Future phases" note saying Option A/B sample-run details and the five failure-mode runbook entries are completed in later phases. Do not reveal trap #2 or trap #3 fixes in this Phase 1 stub. After writing docs, run static checks for missing forbidden files: `test ! -f CLAUDE.md && test ! -f AGENTS.md`. If Plans 01 and 02 have already executed and Docker is available, run the live smoke path from README; otherwise record Docker/live smoke as pending execution in the summary.</action>
  <verify>grep -q 'Phase 1 tracer reference' reference/option-a-sample-run.md && grep -q 'count_per_user' reference/option-a-sample-run.md && grep -q 'Future phases' reference/option-a-sample-run.md && test ! -f CLAUDE.md && test ! -f AGENTS.md</verify>
  <acceptance_criteria>
    - `reference/option-a-sample-run.md` contains `Phase 1 tracer reference`
    - `reference/option-a-sample-run.md` contains `load_top_users`
    - `reference/option-a-sample-run.md` contains `count_per_user`
    - `reference/option-a-sample-run.md` contains `compute_org_summary`
    - `reference/option-a-sample-run.md` contains `Future phases`
    - `CLAUDE.md` does not exist
    - `AGENTS.md` does not exist
  </acceptance_criteria>
  <done>The reference stub supports Phase 1 without spoiling future traps, and forbidden agent-context files are absent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -x tests/bench.sh`
- [ ] `grep -q 'for i in 1 2 3; do' tests/bench.sh`
- [ ] `grep -q 'Prove that hypothesis with data. Run \`bash tests/bench.sh\`' README.md`
- [ ] `grep -q 'This is the tracer slice -- Option A and Option B are not yet present.' README.md`
- [ ] `grep -q 'Phase 1 tracer reference' reference/option-a-sample-run.md`
- [ ] `test ! -f CLAUDE.md && test ! -f AGENTS.md`
- [ ] If Plans 01 and 02 have executed and Docker is available: `bash tests/bench.sh`
</verification>

<success_criteria>

- The bench harness performs exactly three curl requests against localhost:8765.
- README contains all Phase 1 setup prerequisites and verification commands.
- README contains the pivot prompt verbatim.
- README clearly says Option A and Option B are not yet present.
- The reference file is a Phase 1 stub and does not reveal future trap fixes.
- No `CLAUDE.md` or `AGENTS.md` file is shipped.
- No out-of-scope workshop topics are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-03-SUMMARY.md`
</output>
