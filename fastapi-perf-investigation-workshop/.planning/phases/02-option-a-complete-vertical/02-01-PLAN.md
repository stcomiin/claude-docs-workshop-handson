---
phase: 02-option-a-complete-vertical
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/seed/mappings.json
  - docker/seed/seed.py
  - app/routes/dashboard.py
autonomous: true
requirements:
  - REQ-measured-fix-option-a
  - REQ-investigation-discipline-takeaway
user_setup:
  - service: docker
    why: "Live timing verification requires the local Elasticsearch Compose stack."
    local_dev:
      - "Start Docker Desktop or Docker Engine before live smoke checks."
must_haves:
  truths:
    - "The shipped Option A starting point contains all three physical performance traps."
    - "Trap #1 remains the visible per-user `_count` loop in `count_per_user`."
    - "Trap #2 is present because `username` is text + fielddata + keyword subfield and the aggregation targets `username`."
    - "Trap #3 is present because the last-30-day query uses `bool.must` with `now-30d` and `now`."
    - "The endpoint remains functionally correct and emits exactly three top-level timer names."
  artifacts:
    - path: docker/seed/mappings.json
      provides: "Phase 2 username multi-field mapping"
      contains: '"fielddata": true'
    - path: docker/seed/seed.py
      provides: "Stale mapping detection and reseed path"
      contains: "mapping_matches_phase2"
    - path: app/routes/dashboard.py
      provides: "Option A starting endpoint with all physical traps"
      contains: '"field": "username"'
  key_links:
    - from: docker/seed/seed.py
      to: docker/seed/mappings.json
      via: "mapping-shape validation before accepting existing index"
      pattern: "mapping_matches_phase2"
    - from: app/routes/dashboard.py
      to: docker/seed/mappings.json
      via: "username terms aggregation targets parent text field"
      pattern: '"field": "username"'
---

<objective>
Introduce the two hidden physical performance traps required for Phase 2 while
preserving the Phase 1 endpoint contract and the visible N+1 trap.

Purpose: Option A needs the complete as-shipped performance landscape so the
participant sees the N+1 improvement but still has realistic remaining drag.

Output: Updated mapping, seed idempotency, and `compute_org_summary` query shape.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/02-option-a-complete-vertical/02-RESEARCH.md
@.planning/phases/02-option-a-complete-vertical/02-PATTERNS.md
@.planning/phases/02-option-a-complete-vertical/02-VALIDATION.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-01-SUMMARY.md
@.planning/phases/01-tracer-slice-workshop-motion-end-to-end/01-02-SUMMARY.md
@docker/seed/mappings.json
@docker/seed/seed.py
@app/routes/dashboard.py
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-02-01 | Security-disabled local ES could be mistaken for production guidance. | Medium | Runtime changes stay local-only; docs plan labels this as a workshop runtime. |
| T-02-02 | Deliberately slow ES patterns could be copied as recommended code. | Medium | Docs plan calls them intentional traps and Option A fixes only the N+1 trap. |
| T-02-03 | Seed script could delete a non-workshop index. | Low | Script only targets fixed index name `activities` inside the configured workshop ES URL. |

No high-severity threats remain because the endpoint has no user input, no auth
surface, no production deployment path, and only synthetic local seed data.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Change username mapping and reseed stale local indexes</name>
  <files>docker/seed/mappings.json, docker/seed/seed.py</files>
  <read_first>docker/seed/mappings.json, docker/seed/seed.py, .planning/intel/constraints.md, .planning/phases/02-option-a-complete-vertical/02-RESEARCH.md</read_first>
  <action>Update `docker/seed/mappings.json` so `mappings.properties.username` is exactly a text multi-field with fielddata enabled: `"username": {"type": "text", "fielddata": true, "fields": {"keyword": {"type": "keyword"}}}`. Preserve all other existing fields, `number_of_shards`, `number_of_replicas`, and `refresh_interval`. In `docker/seed/seed.py`, add a helper `get_mapping() -> dict[str, object]` that performs `GET /activities/_mapping` and returns decoded JSON, and add `mapping_matches_phase2() -> bool` that returns true only when the existing `username` mapping has `type == "text"`, `fielddata is True`, and nested `fields.keyword.type == "keyword"`. Change `ensure_seeded()` so an existing index is accepted only when both `count_documents() == EXPECTED_COUNT` and `mapping_matches_phase2()` are true; if count is correct but mapping is stale, print `Existing activities index has stale mapping; recreating`, delete the index, recreate it, bulk load, and verify the final count. Do not add external dependencies; keep the script on Python stdlib.</action>
  <verify>
    <automated>grep -q '"fielddata": true' docker/seed/mappings.json && grep -q '"keyword": {"type": "keyword"}' docker/seed/mappings.json && grep -q 'def mapping_matches_phase2' docker/seed/seed.py && grep -q 'stale mapping' docker/seed/seed.py</automated>
  </verify>
  <acceptance_criteria>
    - `docker/seed/mappings.json` contains `"username": {"type": "text", "fielddata": true, "fields": {"keyword": {"type": "keyword"}}}`
    - `docker/seed/seed.py` contains `def mapping_matches_phase2`
    - `docker/seed/seed.py` contains `Existing activities index has stale mapping; recreating`
    - `docker/seed/seed.py` accepts an existing index only when count is 50000 and Phase 2 mapping shape matches
  </acceptance_criteria>
  <done>Fresh and repeated Docker runs both produce the Phase 2 mapping shape.</done>
</task>

<task type="auto">
  <name>Task 2: Move compute_org_summary to the Phase 2 hidden trap shape</name>
  <files>app/routes/dashboard.py</files>
  <read_first>app/routes/dashboard.py, docker/seed/mappings.json, .planning/intel/constraints.md, .planning/phases/02-option-a-complete-vertical/02-RESEARCH.md</read_first>
  <action>Update only `compute_org_summary` and any small private helper it needs. Preserve `list_top_users`, `count_user_activities`, the N+1 loop inside `get_dashboard_summary`, the response TypedDicts, and exactly the three top-level timer blocks. Build the last-30-day aggregation request as `last_30d_body` with `size: 0`, `track_total_hits: true`, `query: {"bool": {"must": [{"range": {"created_at": {"gte": "now-30d", "lt": "now"}}}]}}`, and `aggs` containing `unique_users: {"cardinality": {"field": "user_id"}}` plus `username_distribution: {"terms": {"field": "username", "size": 10}}`. Extract `total_activities_30d` from `hits.total.value` and `unique_users_30d` from the cardinality aggregation. Preserve growth by issuing a prior-period count using `es.count(index=INDEX_NAME, body={"query": {"bool": {"must": [{"range": {"created_at": {"gte": "now-60d", "lt": "now-30d"}}}]}}})` and calculating `growth_rate_vs_prior_30d` the same way as Phase 1. Do not use `username.keyword`, do not use `now-30d/d`, and do not move this date range into `filter` context.</action>
  <verify>
    <automated>grep -q 'now-30d' app/routes/dashboard.py && ! grep -q 'now-30d/d' app/routes/dashboard.py && grep -q '"must"' app/routes/dashboard.py && grep -q '"field": "username"' app/routes/dashboard.py && grep -q 'track_total_hits' app/routes/dashboard.py && pytest && ruff check app && mypy app</automated>
  </verify>
  <acceptance_criteria>
    - `app/routes/dashboard.py` contains `"field": "username"` inside `compute_org_summary`
    - `app/routes/dashboard.py` contains `now-30d` and does not contain `now-30d/d`
    - `app/routes/dashboard.py` contains `"must"` for the last-30-day range query
    - `app/routes/dashboard.py` still contains `timer("load_top_users")`
    - `app/routes/dashboard.py` still contains `timer("count_per_user")`
    - `app/routes/dashboard.py` still contains `timer("compute_org_summary")`
    - `pytest`, `ruff check app`, and `mypy app` exit 0
  </acceptance_criteria>
  <done>The endpoint remains correct while traps #2 and #3 are physically present.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q '"fielddata": true' docker/seed/mappings.json`
- [ ] `grep -q 'def mapping_matches_phase2' docker/seed/seed.py`
- [ ] `grep -q '"field": "username"' app/routes/dashboard.py`
- [ ] `grep -q 'now-30d' app/routes/dashboard.py`
- [ ] `! grep -q 'now-30d/d' app/routes/dashboard.py`
- [ ] `pytest`
- [ ] `ruff check app`
- [ ] `mypy app`
- [ ] If Docker is available: rebuild/reseed and verify `/activities/_mapping` has `username.type == text`
</verification>

<success_criteria>

- The Phase 2 mapping contains `username` as text with fielddata enabled and a keyword subfield.
- Existing local ES indexes with the Phase 1 mapping are recreated automatically.
- `compute_org_summary` targets parent `username`, not `username.keyword`.
- `compute_org_summary` uses query context with non-rounded `now-30d` for the last-30-day calculation.
- The N+1 count loop remains as the visible Option A fix target.
- The endpoint shape and Python gates remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-option-a-complete-vertical/02-01-SUMMARY.md`
</output>
