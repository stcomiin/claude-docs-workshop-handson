---
phase: 02-option-a-complete-vertical
plan: "04"
type: gap_closure
wave: 3
depends_on:
  - "02-UAT"
files_modified:
  - docker/seed/build_seed.py
  - docker/seed/mappings.json
  - app/routes/dashboard.py
  - README.md
  - reference/option-a-sample-run.md
  - .planning/phases/02-option-a-complete-vertical/02-VALIDATION.md
autonomous: true
requirements:
  - REQ-measured-fix-option-a
  - REQ-investigation-discipline-takeaway
user_setup:
  - Docker must be available for live timing calibration.
must_haves:
  truths:
    - "The Phase 2 starting runtime has a clearly slow, measurable baseline and `count_per_user` remains the first bottleneck."
    - "After applying only the Option A N+1 fix in a temporary worktree, the runtime still has material remaining drag from hidden traps #2 and #3."
    - "README/reference timing claims match live calibration evidence or explicitly describe hardware-dependent relative timing."
  artifacts:
    - path: .planning/phases/02-option-a-complete-vertical/02-UAT.md
      provides: "Diagnosed timing gaps from live UAT"
      contains: "The intended 3-6s remaining-drag band was not reproduced."
    - path: README.md
      provides: "Participant-facing timing expectations"
      contains: "Expected Timing Bands"
    - path: reference/option-a-sample-run.md
      provides: "Facilitator-facing timing and intervention guide"
      contains: "Post-fix checks"
---

<objective>
Close the Phase 2 live timing calibration gaps found during UAT.

Purpose: The structural traps are present, but the current Docker/seed/runtime
combination did not reproduce the documented timing story on this VM. Phase 2
should either restore the intended teaching bands on the target image or make
the docs explicitly relative and hardware-dependent.

Output: Tuned runtime/docs plus updated validation evidence showing the
baseline and post-Option-A-fix measurements are trustworthy.
</objective>

<context>
@.planning/phases/02-option-a-complete-vertical/02-UAT.md
@.planning/phases/02-option-a-complete-vertical/02-VALIDATION.md
@.planning/intel/constraints.md
@README.md
@reference/option-a-sample-run.md
@docker/seed/build_seed.py
@docker/seed/mappings.json
@app/routes/dashboard.py
@tests/bench.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalibrate the Phase 2 runtime timing story</name>
  <files>docker/seed/build_seed.py, docker/seed/mappings.json, app/routes/dashboard.py</files>
  <read_first>.planning/phases/02-option-a-complete-vertical/02-UAT.md, .planning/intel/constraints.md, docker/seed/build_seed.py, app/routes/dashboard.py</read_first>
  <action>
    Run live calibration experiments against the Docker runtime and choose the
    smallest runtime/seed adjustment that preserves the Phase 2 trap contracts
    while restoring the teaching story. Keep the distribution near the 50k-doc
    contract unless the constraints are intentionally updated. Candidate knobs:
    `USER_COUNT`, username cardinality/length, `CANDIDATE_USER_LIMIT`, and the
    `username_distribution` aggregation size/shard-size. Preserve these
    invariants: endpoint remains correct; trap #1 is still the visible N+1
    count loop; trap #2 still aggregates on parent `username` with fielddata
    enabled and a keyword subfield available; trap #3 still uses `bool.must`
    with non-rounded `now-30d`/`now`; the endpoint still emits exactly the
    three top-level timer names.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check app tests/test_dashboard.py && uv run mypy app</automated>
    <manual>Run `docker compose -f docker/docker-compose.yml down -v`, `docker compose -f docker/docker-compose.yml up -d --build`, start uvicorn on port 8765, and run `bash tests/bench.sh`. Confirm the starting runtime is slow enough for the workshop and that `count_per_user` dominates the timer output.</manual>
    <manual>In a detached temporary worktree, apply only the participant N+1 fix, restart uvicorn without `--reload`, clear ES caches once, and run `bash tests/bench.sh`. Confirm the post-fix runtime still has material remaining drag from hidden traps #2 and #3.</manual>
  </verify>
  <acceptance_criteria>
    - Starting `bench.sh` results land in the documented 5-10s band on the target calibration environment, or docs are updated to treat absolute values as non-binding.
    - The post-N+1-fix `bench.sh` result lands in the documented 3-6s band on the target calibration environment, or docs are updated to use relative acceptance criteria.
    - Timer logs still point Option A at `count_per_user` before the fix.
    - Timer logs after the Option A fix show meaningful remaining work outside `count_per_user`.
  </acceptance_criteria>
  <done>The runtime can support the Option A timing narrative on target hardware, or the documentation is corrected to match measured reality.</done>
</task>

<task type="auto">
  <name>Task 2: Align docs and validation with measured timing behavior</name>
  <files>README.md, reference/option-a-sample-run.md, .planning/phases/02-option-a-complete-vertical/02-VALIDATION.md</files>
  <read_first>README.md, reference/option-a-sample-run.md, .planning/phases/02-option-a-complete-vertical/02-VALIDATION.md</read_first>
  <action>
    Update the participant README, facilitator reference, and Phase 2
    validation strategy so they match the chosen calibration result. If the
    absolute bands are restored, record the live commands and representative
    results. If the bands remain hardware-dependent, make relative improvement
    the explicit pass condition and mark absolute bands as calibration examples,
    not universal assertions. Preserve the Option A scope guardrails and do not
    reveal hidden fixes in the participant path.
  </action>
  <verify>
    <automated>grep -q 'Option A: 8-step investigation arc' README.md && grep -q 'Now design a measurement whose result would \\*falsify\\*' README.md && grep -q 'fix: replace per-user _count loop with single terms aggregation' README.md</automated>
    <automated>grep -q 'Step 3 not solved in one shot' reference/option-a-sample-run.md && grep -q 'Where Sonnet 4.6 may diverge' reference/option-a-sample-run.md && test ! -f CLAUDE.md && test ! -f AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - README timing expectations no longer overpromise behavior not proven by live UAT.
    - Facilitator reference gives a clear intervention path when local hardware is faster than the calibration target.
    - Phase 2 validation records both the starting and post-fix live timing commands.
    - Hidden fixes remain reserved for later phases.
  </acceptance_criteria>
  <done>Docs and validation evidence agree with the measured runtime behavior.</done>
</task>

</tasks>

<verification>
Before declaring gap closure complete:
- [ ] `uv run pytest -q`
- [ ] `uv run ruff check app tests/test_dashboard.py`
- [ ] `uv run mypy app`
- [ ] `test ! -f CLAUDE.md && test ! -f AGENTS.md`
- [ ] Fresh Docker baseline benchmark captured
- [ ] Temporary-worktree Option A fix benchmark captured
- [ ] README/reference timing language matches the captured results
</verification>
