---
phase: 02-option-a-complete-vertical
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - README.md
  - reference/option-a-sample-run.md
autonomous: true
requirements:
  - REQ-measured-fix-option-a
  - REQ-investigation-discipline-takeaway
user_setup: []
must_haves:
  truths:
    - "A participant can follow the README Option A 8-step arc end to end."
    - "README contains the pivot prompt and falsification prompt verbatim."
    - "README names the expected single participant commit for Option A."
    - "The reference file documents expected Claude behavior, Sonnet divergence, and non-solved step 3 behavior."
    - "Option A docs stop after the N+1 fix and do not teach the hidden fixes."
  artifacts:
    - path: README.md
      provides: "Complete Option A participant path"
      contains: "Option A: 8-step investigation arc"
    - path: reference/option-a-sample-run.md
      provides: "Facilitator Option A answer key"
      contains: "Step 3 not solved in one shot"
  key_links:
    - from: README.md
      to: tests/bench.sh
      via: "participant benchmark command"
      pattern: "bash tests/bench.sh"
    - from: README.md
      to: reference/option-a-sample-run.md
      via: "facilitator troubleshooting reference"
      pattern: "option-a-sample-run"
---

<objective>
Replace the Phase 1 tracer documentation with the complete Option A workshop
path and facilitator reference.

Purpose: Phase 2 is only complete when a participant can run Option A against
the as-shipped endpoint, practice pivot plus falsification, apply exactly the
N+1 fix, and explain the before/after narrative.

Output: Updated `README.md` and `reference/option-a-sample-run.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/intel/context.md
@.planning/phases/02-option-a-complete-vertical/02-RESEARCH.md
@.planning/phases/02-option-a-complete-vertical/02-PATTERNS.md
@.planning/phases/02-option-a-complete-vertical/02-VALIDATION.md
@.planning/phases/02-option-a-complete-vertical/02-01-SUMMARY.md
@README.md
@reference/option-a-sample-run.md
@tests/bench.sh
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-02-06 | README could normalize security-disabled Elasticsearch. | Medium | State that ES security is disabled only for a localhost workshop runtime. |
| T-02-07 | README could teach wrong fixes such as async/await or production tuning. | Medium | Include explicit scope guardrails and the exact one expected Option A fix. |
| T-02-08 | Reference file could spoil hidden fixes and weaken later phases. | Low | Describe remaining drag without naming the `username.keyword` or filter-context fixes. |

No high-severity threats remain because this plan changes docs only and keeps
commands local to the workshop directory.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write the full README Option A participant path</name>
  <files>README.md</files>
  <read_first>README.md, .planning/intel/constraints.md, .planning/intel/context.md, .planning/phases/02-option-a-complete-vertical/02-RESEARCH.md</read_first>
  <action>Rewrite `README.md` around Option A while preserving the existing setup commands that still apply. The README must include: title `# FastAPI Perf Investigation Workshop`; a short statement that this is the Option A path; setup prerequisites for Docker, Python 3.11+, three terminals, Git Bash/WSL on Windows, ports 9200 and 8765, and the `--reload` restart caveat; startup commands `docker compose -f docker/docker-compose.yml up -d --build`, `uv pip install -e .`, `python -m pip install -e .`, `uvicorn app.main:app --port 8765 --reload`, `curl http://localhost:8765/dashboard/summary`, `bash tests/bench.sh`, `pytest`, `ruff check app`, and `mypy app`; expected timing bands `5-10 s` baseline and `3-6 s` after the Option A fix; the exact pivot prompt `Prove that hypothesis with data. Run \`bash tests/bench.sh\`, then read the timing log output from the uvicorn console. Tell me which named timer block dominates the wall-clock time, and quote the numbers verbatim. Only after you have the numbers, propose the fix.`; the exact falsification prompt `Now design a measurement whose result would *falsify* this hypothesis -- not confirm it. If you think \`<phase X>\` dominates, what would you expect to see if it actually didn't? Run that measurement and report what you find. Only proceed to a fix if the falsification attempt fails.`; a section titled `## Option A: 8-step investigation arc` with the eight steps from `CON-option-a-arc`; the expected commit string `fix: replace per-user _count loop with single terms aggregation`; and scope guardrails forbidding async/await refactoring, auth, frontend, production deployment, ES cluster ops, reindex strategy drills, vector search, ML, and ESQL. State that remaining slowness after the Option A fix is intentional and belongs to longer paths, but do not include the hidden fix instructions.</action>
  <verify>
    <automated>grep -q 'Option A: 8-step investigation arc' README.md && grep -q 'Prove that hypothesis with data. Run `bash tests/bench.sh`' README.md && grep -q 'Now design a measurement whose result would \\*falsify\\*' README.md && grep -q 'fix: replace per-user _count loop with single terms aggregation' README.md && grep -q '5-10 s' README.md && grep -q '3-6 s' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Option A: 8-step investigation arc`
    - `README.md` contains `Prove that hypothesis with data. Run \`bash tests/bench.sh\``
    - `README.md` contains `Now design a measurement whose result would *falsify*`
    - `README.md` contains `fix: replace per-user _count loop with single terms aggregation`
    - `README.md` contains `5-10 s`
    - `README.md` contains `3-6 s`
    - `README.md` does not instruct participants to use `username.keyword`
    - `README.md` does not instruct participants to move the date range to filter context
  </acceptance_criteria>
  <done>README supports the complete Option A participant run without spoiling hidden fixes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the facilitator Option A sample run</name>
  <files>reference/option-a-sample-run.md</files>
  <read_first>reference/option-a-sample-run.md, README.md, .planning/intel/constraints.md, .planning/intel/context.md, .planning/phases/02-option-a-complete-vertical/02-RESEARCH.md</read_first>
  <action>Rewrite `reference/option-a-sample-run.md` as a facilitator answer key for Option A. Include sections titled `Expected starting state`, `Step-by-step Option A path`, `Step 3 not solved in one shot`, `Expected Opus 4.7 behavior`, `Where Sonnet 4.6 may diverge`, `Expected Option A commit`, `Post-fix checks`, and `Facilitator intervention points`. Document that step 3 commonly produces a plausible hypothesis without quoted timer data; the facilitator should use the pivot prompt until Claude quotes `load_top_users`, `count_per_user`, and `compute_org_summary` timings and identifies `count_per_user` as dominant. Document that the falsification step should describe what would disprove the N+1 hypothesis before the fix. Document the expected commit exactly as `fix: replace per-user _count loop with single terms aggregation`. Document post-fix checks: `pytest`, `ruff check app`, `mypy app`, manual uvicorn restart, and `bash tests/bench.sh` showing a measurable 5-10 s to 3-6 s improvement band. Mention that remaining slowness is expected after Option A and should not be fixed in this path. Do not include the `username.keyword` or filter-context fix instructions.</action>
  <verify>
    <automated>grep -q 'Step 3 not solved in one shot' reference/option-a-sample-run.md && grep -q 'Where Sonnet 4.6 may diverge' reference/option-a-sample-run.md && grep -q 'fix: replace per-user _count loop with single terms aggregation' reference/option-a-sample-run.md && test ! -f CLAUDE.md && test ! -f AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `reference/option-a-sample-run.md` contains `Step 3 not solved in one shot`
    - `reference/option-a-sample-run.md` contains `Expected Opus 4.7 behavior`
    - `reference/option-a-sample-run.md` contains `Where Sonnet 4.6 may diverge`
    - `reference/option-a-sample-run.md` contains `fix: replace per-user _count loop with single terms aggregation`
    - `reference/option-a-sample-run.md` contains `5-10 s`
    - `reference/option-a-sample-run.md` contains `3-6 s`
    - `CLAUDE.md` does not exist
    - `AGENTS.md` does not exist
  </acceptance_criteria>
  <done>The reference file gives facilitators the Option A answer key and intervention points.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q 'Option A: 8-step investigation arc' README.md`
- [ ] `grep -q 'Now design a measurement whose result would \*falsify\*' README.md`
- [ ] `grep -q 'fix: replace per-user _count loop with single terms aggregation' README.md`
- [ ] `grep -q 'Step 3 not solved in one shot' reference/option-a-sample-run.md`
- [ ] `grep -q 'Where Sonnet 4.6 may diverge' reference/option-a-sample-run.md`
- [ ] `test ! -f CLAUDE.md && test ! -f AGENTS.md`
</verification>

<success_criteria>

- README contains the complete Option A 8-step arc.
- README contains both portable prompts verbatim.
- README identifies the expected one-commit Option A fix and verification gates.
- Reference file documents expected Claude behavior and facilitator interventions.
- Docs preserve hidden traps for later phases and do not teach their fixes.
- No `CLAUDE.md` or `AGENTS.md` exists.
</success_criteria>

<output>
After completion, create `.planning/phases/02-option-a-complete-vertical/02-03-SUMMARY.md`
</output>
