---
phase: 02-option-a-complete-vertical
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - tests/test_dashboard.py
autonomous: true
requirements:
  - REQ-measured-fix-option-a
user_setup: []
must_haves:
  truths:
    - "The endpoint remains correct under fake-client pytest coverage after Phase 2 trap changes."
    - "Tests fail if the username mapping trap is removed."
    - "Tests fail if `compute_org_summary` stops using query context, non-rounded `now-30d`, or parent `username` aggregation."
    - "The full Python quality gate remains green."
  artifacts:
    - path: tests/test_dashboard.py
      provides: "Correctness and Phase 2 trap-shape tests"
      contains: "test_phase2_mapping_contains_username_text_fielddata_keyword_subfield"
  key_links:
    - from: tests/test_dashboard.py
      to: app/routes/dashboard.py
      via: "FakeElasticsearch request-body recording"
      pattern: "search_bodies"
    - from: tests/test_dashboard.py
      to: docker/seed/mappings.json
      via: "JSON mapping inspection"
      pattern: "fielddata"
---

<objective>
Extend automated tests so Phase 2's starting point is both correct and
deliberately slow in the required ways.

Purpose: The workshop cannot rely on prose alone; tests should prevent
executors from accidentally removing the hidden traps while preserving the
response contract.

Output: Updated `tests/test_dashboard.py` with correctness and trap-shape coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/02-option-a-complete-vertical/02-RESEARCH.md
@.planning/phases/02-option-a-complete-vertical/02-PATTERNS.md
@.planning/phases/02-option-a-complete-vertical/02-VALIDATION.md
@.planning/phases/02-option-a-complete-vertical/02-01-SUMMARY.md
@tests/test_dashboard.py
@app/routes/dashboard.py
@docker/seed/mappings.json
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-02-04 | Tests could accidentally hit a developer's real ES cluster. | Medium | Preserve monkeypatching of `dashboard.get_es_client` to a fake client. |
| T-02-05 | Tests could assert production-bad patterns without context. | Low | Test names and docstrings identify the assertions as intentional workshop traps. |

No high-severity threats remain because tests use synthetic fake data and do not
perform network calls.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update fake ES client for Phase 2 query shapes</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, app/routes/dashboard.py, docker/seed/mappings.json, .planning/phases/02-option-a-complete-vertical/02-RESEARCH.md</read_first>
  <action>Modify `FakeElasticsearch` so it records every `search` body in `self.search_bodies` and every `count` body in `self.count_bodies`. Keep the existing top-users search response. Add support for the Phase 2 `compute_org_summary` search response by returning `{"hits": {"total": {"value": 45, "relation": "eq"}}, "aggregations": {"unique_users": {"value": 3}, "username_distribution": {"buckets": [{"key": "user_0001", "doc_count": 18}, {"key": "user_0002", "doc_count": 15}, {"key": "user_0003", "doc_count": 12}]}}}` when the request contains the `unique_users` aggregation. Update `count()` so user-specific count bodies still return `{"count": 18/15/12}` based on `term.user_id`, and prior-period range-only count bodies return `{"count": 30}`. Change the pytest fixture to yield both `TestClient` and the fake ES instance, for example `tuple[TestClient, FakeElasticsearch]`, so tests can inspect recorded request bodies without live Docker.</action>
  <verify>
    <automated>grep -q 'search_bodies' tests/test_dashboard.py && grep -q 'count_bodies' tests/test_dashboard.py && grep -q 'unique_users' tests/test_dashboard.py && pytest tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `self.search_bodies`
    - `tests/test_dashboard.py` contains `self.count_bodies`
    - `tests/test_dashboard.py` fake search response contains `unique_users`
    - `tests/test_dashboard.py` fake count returns `{"count": 30}` for prior-period count
    - `pytest tests/test_dashboard.py -x` exits 0
  </acceptance_criteria>
  <done>The fake client supports Phase 2 runtime requests and exposes request bodies to tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add mapping and query trap-shape assertions</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, app/routes/dashboard.py, docker/seed/mappings.json, .planning/intel/constraints.md, .planning/phases/02-option-a-complete-vertical/02-VALIDATION.md</read_first>
  <action>Keep the four existing correctness tests, updating only fixture unpacking as needed. Add `test_phase2_mapping_contains_username_text_fielddata_keyword_subfield` that reads `docker/seed/mappings.json` with `json.loads(Path("docker/seed/mappings.json").read_text(encoding="utf-8"))` and asserts `username["type"] == "text"`, `username["fielddata"] is True`, and `username["fields"]["keyword"]["type"] == "keyword"`. Add `test_compute_org_summary_uses_query_context_unrounded_now_and_username_text_agg` that calls `/dashboard/summary`, finds the recorded search body containing the `unique_users` aggregation, and asserts `body["query"]["bool"]["must"][0]["range"]["created_at"] == {"gte": "now-30d", "lt": "now"}` and `body["aggs"]["username_distribution"]["terms"]["field"] == "username"`. Also assert the same body does not contain the string `username.keyword` and does not contain `now-30d/d` by running `json.dumps(body)`. Do not assert timing in pytest; timing remains a live bench check.</action>
  <verify>
    <automated>pytest && ruff check app && mypy app</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `def test_phase2_mapping_contains_username_text_fielddata_keyword_subfield`
    - `tests/test_dashboard.py` contains `def test_compute_org_summary_uses_query_context_unrounded_now_and_username_text_agg`
    - `tests/test_dashboard.py` contains `now-30d`
    - `tests/test_dashboard.py` contains `username.keyword`
    - `pytest` exits 0
    - `ruff check app` exits 0
    - `mypy app` exits 0
  </acceptance_criteria>
  <done>Automated tests protect both correctness and the intentional Phase 2 trap shape.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest`
- [ ] `ruff check app`
- [ ] `mypy app`
- [ ] `grep -q 'test_phase2_mapping_contains_username_text_fielddata_keyword_subfield' tests/test_dashboard.py`
- [ ] `grep -q 'test_compute_org_summary_uses_query_context_unrounded_now_and_username_text_agg' tests/test_dashboard.py`
</verification>

<success_criteria>

- The original four response correctness tests still exist and pass.
- New tests assert the Phase 2 username mapping shape.
- New tests assert the Phase 2 query-context, non-rounded date, and parent-username aggregation shape.
- Pytest remains independent of Docker.
- Full Python gates are green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-option-a-complete-vertical/02-02-SUMMARY.md`
</output>
