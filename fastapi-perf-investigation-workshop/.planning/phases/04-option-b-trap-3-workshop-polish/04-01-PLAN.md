---
phase: 04-option-b-trap-3-workshop-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - app/routes/dashboard.py
autonomous: true
requirements:
  - REQ-measured-fix-option-b
  - REQ-portable-prompt-artifacts
user_setup: []
must_haves:
  truths:
    - "README contains the ES DSL primer required by Phase 4."
    - "README contains Option B steps 11 and 12 after the existing steps 9-10."
    - "README names the exact third participant commit."
    - "README contains Appendix B and the stack-agnostic closing restatement."
    - "The shipped dashboard.py does not contain the completed final answers."
  artifacts:
    - path: README.md
      provides: "Participant-facing complete Option B and portable artifact flow"
      contains: "Option B: steps 11-12"
    - path: app/routes/dashboard.py
      provides: "Non-spoiler final-notes template"
      contains: "Final Option B notes"
  key_links:
    - from: README.md
      to: app/routes/dashboard.py
      via: "participant commit 3 target and final notes"
      pattern: "fix: move date range to filter context, round now to day for cache hit"
---

<objective>
Complete the participant-facing workshop flow for Phase 4.

Purpose: README should now teach the full Option B trap #3 path, provide the ES
DSL primer, give Appendix B's translation table, and close with the portable
investigation-discipline restatement. The shipped source should provide only a
non-spoiler place for final notes, not the solved code.

Output: Updated `README.md` and a non-spoiler template comment in
`app/routes/dashboard.py`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/intel/context.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-RESEARCH.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-PATTERNS.md
@README.md
@app/routes/dashboard.py
@tests/bench.sh
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-04-01 | README could normalize security-disabled Elasticsearch as production guidance. | Medium | Preserve the existing localhost-only warning and avoid production deployment advice. |
| T-04-02 | Trap #3 docs could drift into ES cluster operations or reindex strategy. | Medium | Keep the fix to query DSL shape only and repeat out-of-scope guardrails. |
| T-04-03 | A completed source comment could reveal hidden answers before participants investigate. | Medium | Add only a blank/non-answer final-notes template in shipped source. |

No high-severity threats remain because this plan changes local workshop docs
and a non-answer source comment only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the ES DSL primer to README</name>
  <files>README.md</files>
  <read_first>README.md, .planning/intel/constraints.md, .planning/intel/context.md, .planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md</read_first>
  <action>Add a README section titled exactly `## Elasticsearch Query DSL Primer` before the Option A arc. Keep it to one page and define only the DSL used in this workshop. It must include these exact tokens with practical one-sentence explanations: `term`, `terms`, `range`, `match_all`, `bool.must`, `bool.filter`, `aggs.terms`, and `aggs.date_histogram`. It must say `filter` is for yes/no constraints that do not need scoring, and that this matters for cache behavior in the final Option B step. Do not introduce sharding, replicas, auth, vector search, ML, ESQL, production deployment, or reindex strategy content.</action>
  <verify>
    <automated>grep -q '## Elasticsearch Query DSL Primer' README.md && grep -q 'bool.filter' README.md && grep -q 'aggs.date_histogram' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Elasticsearch Query DSL Primer`
    - `README.md` contains `term`
    - `README.md` contains `terms`
    - `README.md` contains `range`
    - `README.md` contains `match_all`
    - `README.md` contains `bool.must`
    - `README.md` contains `bool.filter`
    - `README.md` contains `aggs.terms`
    - `README.md` contains `aggs.date_histogram`
  </acceptance_criteria>
  <done>README has the required ES DSL primer.</done>
</task>

<task type="auto">
  <name>Task 2: Add Option B steps 11-12 and final measurement guidance</name>
  <files>README.md</files>
  <read_first>README.md, app/routes/dashboard.py, .planning/intel/constraints.md, .planning/phases/04-option-b-trap-3-workshop-polish/04-RESEARCH.md</read_first>
  <action>Replace the current stop-after-step-10 ending with a new section titled exactly `## Option B: steps 11-12`. Keep steps 9-10 intact above it. In step 11, instruct the participant to continue only after commit `fix: aggregate on username.keyword to avoid fielddata on text field`, restart uvicorn without `--reload`, run `tests/bench.sh`, and notice repeated runs still do not show stable request-cache hits. Require the diagnosis of two coupled defects: `bool.must` query context and non-rounded `now-30d` date math. Instruct the narrow fix in `app/routes/dashboard.py`: move the 30-day `created_at` range in the `compute_org_summary` aggregation search body from `must` to `filter`, and change `now-30d`/`now` to `now-30d/d`/`now/d`. State that either half alone is insufficient. In step 12, instruct the participant to run `pytest -m "not starting_state"`, `ruff check app`, `mypy app`, restart uvicorn without `--reload`, rerun `tests/bench.sh`, record cold and cached numbers, and create commit `fix: move date range to filter context, round now to day for cache hit`. State the calibration target as `50-150 ms cached / 200-400 ms cold`, with relative improvement plus evidence as binding.</action>
  <verify>
    <automated>grep -q 'Option B: steps 11-12' README.md && grep -q 'fix: move date range to filter context, round now to day for cache hit' README.md && grep -q 'now-30d/d' README.md && grep -q '50-150 ms cached / 200-400 ms cold' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Option B: steps 11-12`
    - `README.md` contains `bool.must`
    - `README.md` contains `bool.filter`
    - `README.md` contains `now-30d/d`
    - `README.md` contains `now/d`
    - `README.md` contains `fix: move date range to filter context, round now to day for cache hit`
    - `README.md` contains `50-150 ms cached / 200-400 ms cold`
    - `README.md` contains `either half alone is insufficient`
  </acceptance_criteria>
  <done>README contains participant-facing Option B trap #3 steps.</done>
</task>

<task type="auto">
  <name>Task 3: Add Appendix B, closing restatement, and non-spoiler source notes</name>
  <files>README.md, app/routes/dashboard.py</files>
  <read_first>README.md, app/routes/dashboard.py, .planning/intel/context.md, .planning/intel/constraints.md, .planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md</read_first>
  <action>Add a README section titled exactly `## Closing Lesson` that includes this stack-agnostic restatement: `Prove that hypothesis with data. Run the perf harness, read the instrumentation output, tell me which named block dominates wall-clock time, and quote the numbers. Only after you have the numbers, propose the fix. Then try to falsify your own hypothesis before you accept it.` Add a README section titled exactly `## Appendix B: Translate To Your World` with a markdown table whose headers are `Workshop primitive (Elasticsearch)`, `Postgres / SQL`, `MongoDB`, and `Spark / Trino / dbt`. Include rows for `tests/bench.sh`, `Profile API / slow log`, named timer blocks, mapping-as-config, query-vs-filter cache context, pivot prompt, and falsification prompt. Add a non-spoiler comment block at the top of `app/routes/dashboard.py` that contains `Final Option B notes` and blank placeholders for before/after evidence, but does not include `username.keyword`, `now-30d/d`, or the exact third commit message.</action>
  <verify>
    <automated>grep -q '## Closing Lesson' README.md && grep -q '## Appendix B: Translate To Your World' README.md && grep -q 'Postgres / SQL' README.md && grep -q 'Final Option B notes' app/routes/dashboard.py && ! head -30 app/routes/dashboard.py | grep -q 'username.keyword'</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Closing Lesson`
    - `README.md` contains `Prove that hypothesis with data`
    - `README.md` contains `## Appendix B: Translate To Your World`
    - `README.md` contains `Workshop primitive (Elasticsearch)`
    - `README.md` contains `Postgres / SQL`
    - `README.md` contains `MongoDB`
    - `README.md` contains `Spark / Trino / dbt`
    - `app/routes/dashboard.py` contains `Final Option B notes`
    - The first 30 lines of `app/routes/dashboard.py` do not contain `username.keyword`
    - The first 30 lines of `app/routes/dashboard.py` do not contain `now-30d/d`
  </acceptance_criteria>
  <done>Portable artifacts and non-spoiler source note template are present.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q '## Elasticsearch Query DSL Primer' README.md`
- [ ] `grep -q 'Option B: steps 11-12' README.md`
- [ ] `grep -q 'fix: move date range to filter context, round now to day for cache hit' README.md`
- [ ] `grep -q '## Closing Lesson' README.md`
- [ ] `grep -q '## Appendix B: Translate To Your World' README.md`
- [ ] `grep -q 'Final Option B notes' app/routes/dashboard.py`
- [ ] `test ! -f CLAUDE.md && test ! -f AGENTS.md`
</verification>

<success_criteria>
- README contains the ES DSL primer.
- README contains Option B steps 11-12 with the exact two-part cache fix and third commit message.
- README contains the closing stack-agnostic restatement and Appendix B table.
- `app/routes/dashboard.py` contains a non-spoiler final-notes template only.
- No `CLAUDE.md` or `AGENTS.md` exists.
</success_criteria>

<output>
After completion, create `.planning/phases/04-option-b-trap-3-workshop-polish/04-01-SUMMARY.md`
</output>
