---
phase: 04-option-b-trap-3-workshop-polish
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - tests/test_dashboard.py
autonomous: true
requirements:
  - REQ-measured-fix-option-b
  - REQ-portable-prompt-artifacts
user_setup:
  - service: docker
    why: "Live post-fix-3 timing confirmation requires the local Elasticsearch Compose stack."
    local_dev:
      - "Start Docker Desktop or Docker Engine before optional live timing verification."
must_haves:
  truths:
    - "Pytest statically verifies Phase 4 README content."
    - "Pytest statically verifies Phase 4 facilitator reference and runbook content."
    - "Pytest preserves the shipped starting-state trap shape."
    - "Verification includes a participant-style post-fix-3 timing path that does not mutate the shipped starting point."
  artifacts:
    - path: tests/test_dashboard.py
      provides: "Phase 4 static docs coverage and starting-state guards"
      contains: "test_phase4_readme_documents_trap3_primer_appendix_and_closing"
  key_links:
    - from: tests/test_dashboard.py
      to: README.md
      via: "static docs coverage"
      pattern: "Option B: steps 11-12"
    - from: tests/test_dashboard.py
      to: reference/option-a-sample-run.md
      via: "facilitator reference coverage"
      pattern: "Failure-mode runbook"
---

<objective>
Add automated guards for Phase 4 docs, source-note scaffolding, and trap
boundaries.

Purpose: Phase 4 should not depend on prose inspection alone. Static tests must
fail if the participant flow, portable artifacts, runbook, source-note template,
or starting-state trap guards disappear.

Output: Updated `tests/test_dashboard.py`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-RESEARCH.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-PATTERNS.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-VALIDATION.md
@README.md
@reference/option-a-sample-run.md
@tests/test_dashboard.py
@app/routes/dashboard.py
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-04-07 | Tests could require live ES credentials in normal CI. | Medium | Keep pytest static/fake-client only; live ES timing stays manual or temp-worktree verification. |
| T-04-08 | Tests could accidentally bless solved starting code. | Medium | Preserve `starting_state` assertions for parent `username`, `bool.must`, and unrounded `now-30d`. |
| T-04-09 | Tests could miss portable artifacts and let README drift. | Low | Assert exact headings and key strings for primer, Appendix B, and closing restatement. |

No high-severity threats remain because the automated test work is local and
does not add network calls.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 README static assertions</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, README.md, .planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md</read_first>
  <action>Add a test function named exactly `test_phase4_readme_documents_trap3_primer_appendix_and_closing`. Read `README.md` with `project_file("README.md").read_text(encoding="utf-8")`. Assert these exact strings are present: `## Elasticsearch Query DSL Primer`, `bool.filter`, `aggs.date_histogram`, `Option B: steps 11-12`, `fix: move date range to filter context, round now to day for cache hit`, `now-30d/d`, `50-150 ms cached / 200-400 ms cold`, `## Closing Lesson`, `Prove that hypothesis with data`, `## Appendix B: Translate To Your World`, `Postgres / SQL`, `MongoDB`, and `Spark / Trino / dbt`. Update the existing `test_phase3_readme_documents_profile_slow_log_and_keyword_fix` so it no longer globally asserts that README lacks `fix: move date range to filter context, round now to day for cache hit` or `now-30d/d`; those strings are expected after Phase 4. Keep its positive assertions for steps 9-10.</action>
  <verify>
    <automated>pytest tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `def test_phase4_readme_documents_trap3_primer_appendix_and_closing`
    - `tests/test_dashboard.py` contains `Option B: steps 11-12`
    - `tests/test_dashboard.py` contains `## Appendix B: Translate To Your World`
    - `test_phase3_readme_documents_profile_slow_log_and_keyword_fix` no longer globally rejects `now-30d/d`
    - `pytest tests/test_dashboard.py -x` exits 0
  </acceptance_criteria>
  <done>Pytest protects Phase 4 README coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add facilitator reference and source-template assertions</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, reference/option-a-sample-run.md, app/routes/dashboard.py, .planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md</read_first>
  <action>Add a test function named exactly `test_phase4_reference_documents_trap3_and_failure_modes`. Assert `reference/option-a-sample-run.md` contains `Option B steps 11-12`, `Expected trap #3 investigation path`, `Completed dashboard.py comment example`, `fix: move date range to filter context, round now to day for cache hit`, `## Failure-mode runbook`, `Claude finds the cause too fast`, `Fix does not measurably help`, `Bench numbers do not change`, `Docker image will not start or ES is not green/yellow`, and `Warm request cache masks trap #3`. Update the existing `test_phase3_reference_documents_trap2_path_and_trap3_boundary` so it no longer globally asserts that the reference lacks `fix: move date range to filter context, round now to day for cache hit` or `now-30d/d`; those strings are expected after Phase 4. Add a test named exactly `test_phase4_dashboard_has_non_spoiler_final_notes_template` that reads the first 30 lines of `app/routes/dashboard.py`, asserts they contain `Final Option B notes`, and asserts they do not contain `username.keyword`, `now-30d/d`, or `fix: move date range to filter context, round now to day for cache hit`.</action>
  <verify>
    <automated>pytest tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` contains `def test_phase4_reference_documents_trap3_and_failure_modes`
    - `tests/test_dashboard.py` contains `def test_phase4_dashboard_has_non_spoiler_final_notes_template`
    - `tests/test_dashboard.py` contains `Warm request cache masks trap #3`
    - `test_phase3_reference_documents_trap2_path_and_trap3_boundary` no longer globally rejects `now-30d/d`
    - `pytest tests/test_dashboard.py -x` exits 0
  </acceptance_criteria>
  <done>Pytest protects facilitator reference and non-spoiler source-template coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve starting-state guards and run full verification</name>
  <files>tests/test_dashboard.py</files>
  <read_first>tests/test_dashboard.py, app/routes/dashboard.py, README.md, reference/option-a-sample-run.md, pyproject.toml</read_first>
  <action>Keep the existing `@pytest.mark.starting_state` test for `compute_org_summary` intact and update only if needed to account for non-spoiler comments. It must continue asserting the shipped runtime body uses `body["query"]["bool"]["must"][0]["range"]["created_at"] == {"gte": "now-30d", "lt": "now"}` and `body["aggs"]["username_distribution"]["terms"]["field"] == "username"`. Ensure `test_phase3_no_agent_instruction_files_ship` still checks no `CLAUDE.md` or `AGENTS.md`. Run `pytest`, `ruff check app`, and `mypy app`. When Docker is available during execution, perform a temp-branch or temp-worktree participant-style check for post-fix-3 timing and record the result in the summary without committing solved source.</action>
  <verify>
    <automated>pytest && ruff check app && mypy app</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_dashboard.py` still contains `@pytest.mark.starting_state`
    - `tests/test_dashboard.py` still contains `"gte": "now-30d"`
    - `tests/test_dashboard.py` still contains `"field"] == "username"`
    - `pytest` exits 0
    - `ruff check app` exits 0
    - `mypy app` exits 0
  </acceptance_criteria>
  <done>Full static verification is green and starting-state trap guards remain.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_dashboard.py -x`
- [ ] `pytest`
- [ ] `ruff check app`
- [ ] `mypy app`
- [ ] `grep -q 'test_phase4_readme_documents_trap3_primer_appendix_and_closing' tests/test_dashboard.py`
- [ ] `grep -q 'test_phase4_reference_documents_trap3_and_failure_modes' tests/test_dashboard.py`
- [ ] `grep -q 'test_phase4_dashboard_has_non_spoiler_final_notes_template' tests/test_dashboard.py`
</verification>

<success_criteria>
- Tests assert README covers the primer, steps 11-12, commit 3, Appendix B, and closing restatement.
- Tests assert the reference covers trap #3 and all five failure modes.
- Tests assert the source template is non-spoiler.
- Existing starting-state guards still prove shipped trap #3 is present.
- `pytest`, `ruff check app`, and `mypy app` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-option-b-trap-3-workshop-polish/04-03-SUMMARY.md`
</output>
