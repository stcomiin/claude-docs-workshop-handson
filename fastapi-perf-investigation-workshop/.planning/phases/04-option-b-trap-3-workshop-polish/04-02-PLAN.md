---
phase: 04-option-b-trap-3-workshop-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/option-a-sample-run.md
autonomous: true
requirements:
  - REQ-measured-fix-option-b
  - REQ-portable-prompt-artifacts
user_setup: []
must_haves:
  truths:
    - "The facilitator reference documents Option B steps 11 and 12."
    - "The facilitator reference contains the completed comment-block example."
    - "The facilitator reference contains all five failure-mode runbook entries."
    - "The facilitator reference keeps troubleshooting scoped to the local workshop runtime."
  artifacts:
    - path: reference/option-a-sample-run.md
      provides: "Facilitator answer key and failure-mode runbook for complete Option B"
      contains: "Failure-mode runbook"
  key_links:
    - from: reference/option-a-sample-run.md
      to: README.md
      via: "facilitator explanation for participant steps 11-12"
      pattern: "Option B steps 11-12"
---

<objective>
Extend the facilitator reference through the complete Option B path.

Purpose: Facilitators need the expected trap #3 investigation, model divergence
guidance, completed comment-block example, post-fix-3 verification, and all five
runbook entries required by the source SPEC.

Output: Updated `reference/option-a-sample-run.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/constraints.md
@.planning/intel/context.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-RESEARCH.md
@.planning/phases/04-option-b-trap-3-workshop-polish/04-PATTERNS.md
@README.md
@reference/option-a-sample-run.md
@app/routes/dashboard.py
</context>

<threat_model>
## Security Threat Model

**ASVS level:** L1 local workshop runtime
**Block on:** high severity

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-04-04 | Runbook commands could be mistaken for production ES guidance. | Medium | Label Docker, cache, and slow-log recovery as localhost workshop support. |
| T-04-05 | Facilitator reference could encourage broad rewrites when fixes disappoint. | Medium | Each failure mode redirects to measurement, narrow fixes, or reset/restart steps. |
| T-04-06 | Model divergence notes could normalize accepting guesses. | Medium | Require quoted evidence before facilitator accepts the diagnosis. |

No high-severity threats remain because the file is facilitator-only local
workshop guidance and does not change runtime behavior.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the trap #3 facilitator path</name>
  <files>reference/option-a-sample-run.md</files>
  <read_first>reference/option-a-sample-run.md, README.md, .planning/intel/constraints.md, .planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md</read_first>
  <action>Add a new section titled exactly `## Option B steps 11-12` after the existing trap #3 boundary. Include subsections `Expected trap #3 investigation path`, `Cache evidence`, `Expected Option B commit 3`, `Post-fix-3 checks`, and `Where models may diverge`. Document this sequence: after commit 2, repeated bench/request runs do not show request-cache hits; the accepted diagnosis is `bool.must` query context plus unrounded `now-30d`; the fix is to move the 30-day range to `bool.filter` and round to `now-30d/d` and `now/d`; either half alone is insufficient; the exact third commit is `fix: move date range to filter context, round now to day for cache hit`; the calibration target is `50-150 ms cached / 200-400 ms cold`.</action>
  <verify>
    <automated>grep -q 'Option B steps 11-12' reference/option-a-sample-run.md && grep -q 'Expected trap #3 investigation path' reference/option-a-sample-run.md && grep -q 'fix: move date range to filter context, round now to day for cache hit' reference/option-a-sample-run.md</automated>
  </verify>
  <acceptance_criteria>
    - `reference/option-a-sample-run.md` contains `Option B steps 11-12`
    - `reference/option-a-sample-run.md` contains `Expected trap #3 investigation path`
    - `reference/option-a-sample-run.md` contains `Cache evidence`
    - `reference/option-a-sample-run.md` contains `now-30d/d`
    - `reference/option-a-sample-run.md` contains `fix: move date range to filter context, round now to day for cache hit`
    - `reference/option-a-sample-run.md` contains `50-150 ms cached / 200-400 ms cold`
  </acceptance_criteria>
  <done>Reference contains the facilitator trap #3 path.</done>
</task>

<task type="auto">
  <name>Task 2: Add completed comment-block example and model guidance</name>
  <files>reference/option-a-sample-run.md</files>
  <read_first>reference/option-a-sample-run.md, app/routes/dashboard.py, .planning/intel/context.md, .planning/phases/04-option-b-trap-3-workshop-polish/04-CONTEXT.md</read_first>
  <action>Add a subsection titled exactly `Completed dashboard.py comment example`. Include an example comment block that documents all three fixes in order: `fix: replace per-user _count loop with single terms aggregation`, `fix: aggregate on username.keyword to avoid fielddata on text field`, and `fix: move date range to filter context, round now to day for cache hit`. Add model guidance saying Opus 4.7 should usually follow the cache-evidence prompt, while Sonnet 4.6 may over-focus on code-reading or broad ES tuning. Include the phrase `quoted cache evidence` and direct facilitators to reject async/await, sharding, auth, frontend, reindex drills, vector/ML/ESQL, and broad rewrites.</action>
  <verify>
    <automated>grep -q 'Completed dashboard.py comment example' reference/option-a-sample-run.md && grep -q 'quoted cache evidence' reference/option-a-sample-run.md && grep -q 'Sonnet 4.6' reference/option-a-sample-run.md</automated>
  </verify>
  <acceptance_criteria>
    - `reference/option-a-sample-run.md` contains `Completed dashboard.py comment example`
    - `reference/option-a-sample-run.md` contains `fix: replace per-user _count loop with single terms aggregation`
    - `reference/option-a-sample-run.md` contains `fix: aggregate on username.keyword to avoid fielddata on text field`
    - `reference/option-a-sample-run.md` contains `fix: move date range to filter context, round now to day for cache hit`
    - `reference/option-a-sample-run.md` contains `quoted cache evidence`
    - `reference/option-a-sample-run.md` contains `Opus 4.7`
    - `reference/option-a-sample-run.md` contains `Sonnet 4.6`
  </acceptance_criteria>
  <done>Reference gives facilitators the final comment and model intervention guidance.</done>
</task>

<task type="auto">
  <name>Task 3: Add all five failure-mode runbook entries</name>
  <files>reference/option-a-sample-run.md</files>
  <read_first>reference/option-a-sample-run.md, .planning/intel/constraints.md, .planning/intel/context.md</read_first>
  <action>Add a section titled exactly `## Failure-mode runbook`. It must include five subsections with these exact headings: `Claude finds the cause too fast`, `Fix does not measurably help`, `Bench numbers do not change`, `Docker image will not start or ES is not green/yellow`, and `Warm request cache masks trap #3`. For each entry include: trigger, facilitator response, evidence to collect, and recovery. The warm-cache entry must mention clearing or restarting enough ES/app state to re-establish cold vs cached behavior. Keep all entries scoped to local workshop troubleshooting.</action>
  <verify>
    <automated>grep -q '## Failure-mode runbook' reference/option-a-sample-run.md && grep -q 'Claude finds the cause too fast' reference/option-a-sample-run.md && grep -q 'Warm request cache masks trap #3' reference/option-a-sample-run.md</automated>
  </verify>
  <acceptance_criteria>
    - `reference/option-a-sample-run.md` contains `## Failure-mode runbook`
    - `reference/option-a-sample-run.md` contains `Claude finds the cause too fast`
    - `reference/option-a-sample-run.md` contains `Fix does not measurably help`
    - `reference/option-a-sample-run.md` contains `Bench numbers do not change`
    - `reference/option-a-sample-run.md` contains `Docker image will not start or ES is not green/yellow`
    - `reference/option-a-sample-run.md` contains `Warm request cache masks trap #3`
  </acceptance_criteria>
  <done>Reference contains the complete facilitator failure-mode runbook.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -q 'Option B steps 11-12' reference/option-a-sample-run.md`
- [ ] `grep -q 'Completed dashboard.py comment example' reference/option-a-sample-run.md`
- [ ] `grep -q '## Failure-mode runbook' reference/option-a-sample-run.md`
- [ ] `grep -q 'Warm request cache masks trap #3' reference/option-a-sample-run.md`
- [ ] `grep -q 'fix: move date range to filter context, round now to day for cache hit' reference/option-a-sample-run.md`
</verification>

<success_criteria>
- Reference covers Option B steps 11-12 and the exact commit 3 path.
- Reference includes the completed final comment-block example.
- Reference includes Opus/Sonnet divergence guidance for trap #3.
- Reference contains all five failure-mode runbook entries.
</success_criteria>

<output>
After completion, create `.planning/phases/04-option-b-trap-3-workshop-polish/04-02-SUMMARY.md`
</output>
